# Backend API server (server/index.js) — copy to server/.env for local use.
DATABASE_URL=your_database_connection_string
PORT=4000

# Frontend (Vite) — copy to .env.local at the project root if the API runs
# somewhere other than http://localhost:4000.
VITE_API_BASE_URL=http://localhost:4000
